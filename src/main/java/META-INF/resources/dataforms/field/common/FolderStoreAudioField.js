/**
 * @fileOverview {@link FolderStoreAudioField}クラスを記述したファイルです。
 */

'use strict';

import { AudioField } from './AudioField.js';

/**
 * @class FolderStoreAudioField
 *
 * @extends AudioField
 */
export class FolderStoreAudioField extends AudioField {
	/**
	 * コンストラクタ。
	 */
	constructor() {
		super();
	}

	/**
	 * HTMLエレメントとの対応付けを行います。
	 */
	attach() {
		super.attach();
	}
}

