/**
 * @fileOverview {@link BlobStoreAudioField}クラスを記述したファイルです。
 */

'use strict';

import { AudioField } from './AudioField.js';

/**
 * @class BlobStoreAudioField
 *
 * @extends AudioField
 */
export class BlobStoreAudioField extends AudioField {
	/**
	 * コンストラクタ。
	 */
	constructor() {
		super();
	}

	/**
	 * HTMLエレメントとの対応付けを行います。
	 */
	attach() {
		super.attach();
	}
}

