/**
 * @fileOverview {@link ExistingFolderField}クラスを記述したファイルです。
 */

'use strict';

import { FolderField } from './FolderField.js';

/**
 * @class ExistingFolderField
 *
 * @extends FolderField
 */
export class ExistingFolderField extends FolderField {
	/**
	 * コンストラクタ。
	 */
	constructor() {
		super();
	}

	/**
	 * HTMLエレメントとの対応付けを行います。
	 */
	attach() {
		super.attach();
	}

}

